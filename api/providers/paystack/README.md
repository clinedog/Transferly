# Paystack Provider Module

Transferly keeps Paystack as a provider-specific service module while preserving shared provider contracts and operational safety.

This folder is reserved for Paystack-specific schemas, status mapping, webhook normalization, readiness rules, and sandbox-only fixtures. Do not store credentials, live webhook payloads, bearer tokens, or production data here.
