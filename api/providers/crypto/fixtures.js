const fixtures = Object.freeze({
  sandbox: {
    enabled: false,
    note: 'Crypto fixtures are intentionally disabled by default and never contain wallet secrets, private keys, mnemonics, or raw provider payloads.'
  }
});

module.exports = {
  fixtures
};
