# Crypto Provider Module

Transferly keeps crypto payment operations as a provider-specific service module while preserving shared provider contracts and operational safety.

This folder is reserved for crypto-specific schemas, status mapping, webhook normalization, readiness rules, and sandbox-only fixtures. Do not store private keys, wallet seeds, credentials, live webhook payloads, bearer tokens, or production data here.
