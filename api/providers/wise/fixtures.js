const fixtures = Object.freeze({
  sandbox: {
    enabled: false,
    note: 'Wise fixtures are intentionally disabled by default and never contain live credentials or raw provider payloads.'
  }
});

module.exports = {
  fixtures
};
