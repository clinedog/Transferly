# Wise Provider Module

Transferly keeps Wise as a provider-specific service module while preserving shared provider contracts and operational safety.

This folder is reserved for Wise-specific schemas, status mapping, webhook normalization, readiness rules, and sandbox-only fixtures. Do not store credentials, live webhook payloads, bearer tokens, or production data here.
